# Draft for the spatialwarnings package

Note that this is a Sweave document, so please edit the file "article.Rnw" and 
compile it into a pdf document using: 
  
  `R CMD Sweave ./article.Rnw && pdflatex article.tex`
  
  
